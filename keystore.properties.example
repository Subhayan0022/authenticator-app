# Copy to keystore.properties and fill in. That file is gitignored and must
# never be committed. Without it the release build is simply unsigned, which
# is what CI and F-Droid do — they sign with their own keys.
storeFile=lockstep-release.jks
storePassword=
keyAlias=lockstep
keyPassword=
